version: '3.8'

services:
  personnel-app:
    build:
      context: ../..
      dockerfile: ./apps/personnel-app/Dockerfile
    ports:
      - "3001:3001"
    environment:
      - VITE_API_BASE
      - VITE_VIDEO_SDK_TOKEN
      - VITE_WEBSOCKET_BASE
